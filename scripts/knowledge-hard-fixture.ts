import type { NormalizedDocument } from '../src/shared/contracts'

/** Frozen, self-authored difficult negatives; none of the expected facts come from model outputs. */
export const hardFixtureVersion = 'harbour-handbook-20260909-v1'
export const hardFacts = [
  ['定义与权限', '双钥封存是把开启资料的权力分给两位不同值守人；两人不得由同一人兼任。', 'f01'],
  ['期限与版本', '白鹭站现行封存规则：首次复查在封存后十八小时内完成，起点是封条登记时刻，不是人员到岗时刻。', 'f02'],
  ['旧规档案', '旧版白鹭站制度曾要求十二小时复查，已于第三次修订时废止，不能再作现行期限。', 'f03'],
  ['异站制度', '苍鹭站仍以十二小时为首次复查期限；苍鹭站不是白鹭站，规则不能跨站套用。', 'f04'],
  ['紧急授权', '应急旁路只在电源中断、现场有人身危险、双签人无法到场且措施可以撤回时启用，四项必须同时成立。', 'f05'],
  ['紧急授权', '通信中断不等于电源中断；即使联络不上签署人，只要供电正常，就不能按应急旁路放行。', 'f06'],
  ['审计与追认', '应急旁路启用后须在六小时内交给独立审计员追认；危险解除也不能免除这一义务。', 'f07'],
  ['独立性', '独立见证指记录来源互不抄录且采样时钟分别校准。设备型号不同，并不能单独证明来源独立。', 'f08'],
  ['独立性', '复印件与其原件属于同一条证据链，即使分别保管、分别编号，也不能算两份独立见证。', 'f09'],
  ['公式与边界', '置信余量 M = (A - B) / C，只有 C > 0 且 A >= B 时可计算；M 没有物理量纲。', 'f10'],
  ['公式与边界', '当 C = 0 时应登记“不可计算”，禁止以零、无穷大或约定常数代替置信余量。', 'f11'],
  ['表格与分类', '现行白鹭站蓝组：普通样本保留三十一天、复核阈值为零点七二；异常样本保留九十三天、复核阈值为零点八八。', 'f12'],
  ['表格与分类', '现行白鹭站青组：普通样本保留四十一天、复核阈值为零点六二；异常样本保留一百零三天、复核阈值为零点九八。蓝组与青组不可混用。', 'f13'],
  ['撤回与删除', '撤销共享只关闭其他人的访问权限，本地原始记录仍然保留；销毁原始记录必须另走双签批准程序。', 'f14'],
  ['撤回与删除', '匿名化会删除人员姓名与号码，但原始事件顺序必须保留；不能通过调整事件先后来美化审核记录。', 'f15'],
  ['脚注与勘误', '冻结时钟规则仅适用于迁移中的只读镜像，在线主库的复查计时绝不能随镜像一起暂停。', 'f16'],
  ['脚注与勘误', '蓝组异常样本在九十三天届满时如仍有申诉，应继续保留到申诉结案后三天，再执行删除审批。', 'f17'],
  ['恢复与副本', '验收副本必须逐段核对校验和并试读随机选出的五段。文件能打开或总页数一致，都不能代替这些检查。', 'f18'],
  ['恢复与副本', '迁移失败时先恢复原索引，再核对原始记录的校验和，最后重建派生缓存；不得从派生缓存反向覆盖原始记录。', 'f19'],
  ['期限与版本', '本手册中的“小时”均指连续自然小时，包含夜间与节假日；“工作日”只在明确写出该词的条款中使用。', 'f20']
] as const

export const hardQuestions = [
  ['H01', '白鹭站封存后最晚多久要第一次复查，从何时计时？旧制度还算数吗？', ['f02', 'f03'], ['十八小时', '封条', '废止']],
  ['H02', '一人拿两把开启钥匙能满足双钥制度吗？', ['f01'], ['不同', '不能']],
  ['H03', '不同厂家的设备提供了两份记录，就足以称作独立见证吗？还缺什么？', ['f08'], ['互不抄录', '校准']],
  ['H04', '只是电话失联但电还正常，能否启用应急旁路？', ['f05', 'f06'], ['不能', '电源']],
  ['H05', '险情结束以后，应急旁路是否就不必补办手续？由谁在多长时间内做？', ['f07'], ['六小时', '独立审计']],
  ['H06', '计算置信余量时分母为零应该记录什么？', ['f11'], ['不可计算']],
  ['H07', '白鹭站蓝组异常样本的留存期和门槛分别是多少？', ['f12'], ['九十三', '零点八八']],
  ['H08', '把别人共享权限收回来等于抹掉本地原件吗？', ['f14'], ['保留', '双签']],
  ['H09', '白鹭与苍鹭两个站点的首次复查期限一样吗？', ['f02', 'f04'], ['十八', '十二']],
  ['H10', '应急旁路的全部门槛和启用后的追认要求是什么？', ['f05', 'f07'], ['四', '六小时', '独立审计']],
  ['H11', '原件复印后交给另一部门并另编号码，可否作为第二个独立来源？', ['f09'], ['同一', '不能']],
  ['H12', 'M的公式是什么，有哪些数学前提，带单位吗？', ['f10'], ['A', 'B', 'C', '量纲']],
  ['H13', '蓝组普通样本与青组普通样本的保存期限差多少天？', ['f12', 'f13'], ['十']],
  ['H14', '蓝组异常样本过了九十三天，但申诉没结束，可以直接删除吗？', ['f12', 'f17'], ['结案', '三天', '审批']],
  ['H15', '镜像迁移卡住时，能顺带把线上库的复查倒计时停掉吗？', ['f16'], ['不能', '主库']],
  ['H16', '迁移后PDF能正常打开且页数相同，是否就验收合格？还应核对什么？', ['f18'], ['校验和', '五段']],
  ['H17', '迁移出错后恢复步骤应当怎样排序？可用缓存覆盖原件吗？', ['f19'], ['原索引', '校验和', '派生缓存', '不得']],
  ['H18', '去标识化以后，为了审核更顺利能不能重排事件时间线？', ['f15'], ['顺序', '不能']],
  ['H19', '白鹭站周六晚上封存，十八小时的首次复查期限是否要等到工作日才计时？', ['f02', 'f20'], ['自然小时', '节假日']],
  ['H20', 'M的输入A小于B，而C大于零，能否照公式算负的置信余量？', ['f10'], ['不能', 'A']],
  ['H21', '这套制度让事故率下降了百分之几？', [], []],
  ['H22', '手册作者的私人电话号码是多少？', [], []],
  ['H23', '海鸥站红组的样本需要留存多少天？', [], []],
  ['H24', '如果A、B、C都没有测到，手册提供的默认M数值是多少？', [], []]
].map(([id, question, expected, answerTerms], index) => ({ id: id as string, question: question as string,
  expected: expected as string[], answerTerms: answerTerms as string[], split: index < 8 ? 'development' : 'holdout' }))

export function createHardDocument(): NormalizedDocument {
  const titles = [...new Set(hardFacts.map((fact) => fact[0]))]
  const nodes = titles.map((title, index) => ({ id: 'c' + index, title, order: index, level: 1, parentId: null, anchor: 'txt:0:1', kind: 'section' as const }))
  const units: NormalizedDocument['units'] = []
  let offset = 0
  const add = (nodeId: string, id: string, text: string) => {
    const end = offset + Array.from(text).length
    units.push({ id, nodeId, order: units.length, kind: 'paragraph', text, sources: [{ anchor: `txt:${offset}:${end}`, start: offset, end, precision: 'text' }], relatedIds: [], searchable: true })
    offset = end + 2
  }
  for (const [chapter, node] of nodes.entries()) {
    for (let index = 0; index < 24; index++) {
      if (index === 7) for (const [title, fact, id] of hardFacts) if (title === node.title) add(node.id, id, fact)
      const subject = ['训练场', '演示仓', '样机室', '旧例集'][index % 4]
      const hours = [12, 24, 36, 48, 72, 96][index % 6]
      const text = `演练附录${chapter + 1}-${index + 1}，仅用于${subject}的虚构工单，不是生产站点的现行制度。` +
        `${subject}记录中常见的关键词包括封存、复查、双钥、独立见证、样本、蓝组、青组、旁路、申诉以及数据迁移。这里的首次复查演练使用${hours}小时，留存演练使用${31 + index * 3}天；这些数字只用于计时器的界面测试，不对应白鹭站或者苍鹭站。` +
        '练习人员先检查表头和记录日期，再写下自己采用的版本。某些示例会故意把到岗时刻当作起点，以帮助审核者发现错误；另一些示例把副本的文件名当成独立来源，或者把文件能打开当作数据完整。完成工单并不意味着这些推理成立，解释示例时必须保留“演练”限定。' +
        '当演示设备断电，值守人只在断开的测试回路内检查告警；正式系统必须查阅对应条款。本附录没有修改双签权限，也没有规定任何默认数学常数。每次试读都记录打开顺序和哈希值，但不得借用训练记录推断实际事故比例。姓名与电话号码均为未填写字段，不含个人信息。' +
        '整理员应当保留题目的原始表述、失败结果及校正过程。相似用词不能证明情境相同，旧值不能覆盖现行值，关联记录也不能代替直接证据。跨章检索需要查明规则所属对象和生效范围；在缺少依据的时候应当明确保留空白，而不借鉴隔壁工作站的数值。'
      add(node.id, `n${chapter}-${index}`, text)
    }
  }
  return { version: 2, nodes, units, diagnostics: [] }
}
