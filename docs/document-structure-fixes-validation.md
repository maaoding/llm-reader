# 文档结构审查修复验收

日期：2026-09-08–09。基于当前未提交工作区增量修复，应用版本仍为 0.4.0。

## 修复内容

1. 关联原文只占用现有入选证据之外的剩余名额。使用完整入选集合排除重复内容，保留直接命中、章节代表及选区邻近原文的顺序和优先级。补充原文仍最多 12 条，原有预算分配和逐轮引用编号逻辑保持兼容。
2. MinerU 主体文字为空时，独立保留已有表题、表注、代码说明及图片/图表说明，保存真实页码和坐标。没有主体的条目不会生成空原文或悬空关联；同一条目中存留的说明使用实际单元建立关联。表格主体缺失时保留表格降级诊断。

本次源代码仅修改 `src/main/book-analysis.ts` 和 `src/main/document-normalizer.ts`，回归用例补充在两个已有单元测试文件中。未更改数据结构、配置、接口或依赖，也未自动重建用户的现有缓存。

## 回归场景

- 本段/全书 × 重排关闭/开启：六章代表均带脚注，同时保留规划章节之外排名第一的直接答案；已被直接命中的脚注不重复、不降为扩展证据。正常与缩减输入预算下核对选区、直接答案、跨章原文及引用编号。
- 表格、代码、图片、图表主体为空：分别验证有标题和无标题的说明保留、可检索属性、Unicode、原文页码和坐标，以及无悬空关联、无空正文和诊断。
- 显式重新准备 PDF：从保留的原始 MinerU 解析产物恢复旧结构遗漏的说明，重新进入全文索引；确认没有上传、轮询或模型调用。

新增 8 个场景先在修复前失败，修复后通过；另补充 1 个原始解析缓存复用场景，共新增 9 项单元测试。首次完整检查发现新增测试的换行 lint 错误、第二次发现测试处理器枚举不匹配，均已修正；原始失败日志保留。

## 验证记录

本次记录目录：`output/document-structure-fixes-20260908/`。`baseline/` 保存开始修改前的四个相关源文件，`status-before.txt` 保存原有工作区状态；`regression-before.log` 和 `regression-after.log` 分别记录修复前后针对性测试。

`pnpm test:all` 已通过，退出码 0：lint、typecheck、34 个文件中的 286 项单元测试、Electron 应用编译，以及 72 项 Electron E2E；另有 1 项依赖用户提供个人 PDF 的既有用例跳过。完整记录为 `test-all-serial.log`。桌面流程覆盖明暗主题、正常/最小窗口、原文准备、问答、PDF 跨页引用和归档恢复。

2026-09-09 尝试执行安装包命令时，pnpm 命令解析报错，实际打包尚未开始。随后用户明确要求先不构建安装包，已停止打包且不再重试；`build-win.log` 保留该次失败。本次修复不能沿用前一阶段的成功安装包作为最终构建验收。

## 已有数据与验证边界

此前已经准备过且受文字遗漏问题影响的 PDF，需由用户点击“重新准备原文”才能更新其结构与全文索引。服务配置未变且原始解析产物缓存可用时复用本地产物；相关章节笔记及语义索引按现有流程待重建，旧归档保留。问答证据优先级修复无需重新准备原文。

本次验证使用本地模拟接口和自建样本，不代表新版 MinerU/Docling 的真实 OCR 质量或供应商问答效果已验证。本次不安装或发布；源码提交推送按用户后续授权执行。整个会话的完成范围与剩余事项见 [全书上下文交付状态](whole-book-context-status.md)。
