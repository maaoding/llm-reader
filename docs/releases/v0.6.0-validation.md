# v0.6.0 发布准备记录

记录日期：2026-09-25。当前为发布候选版；公开的最新 GitHub Release 仍为 v0.5.0，尚未创建 `v0.6.0` 标签或 Release。

## 已完成验证

- `package.json` 已更新至 `0.6.0`，发布说明草稿见 [v0.6.0.md](v0.6.0.md)。
- `pnpm test:all` 通过：lint、typecheck、408 项单元测试和 Electron E2E 98 项通过、1 项跳过。跳过项需要本机复杂学术 PDF 测试样本。
- `pnpm build:win --publish never` 通过，生成 Windows NSIS 安装包、blockmap 和 `latest.yml`。
- 对打包后的 `win-unpacked` 应用运行了 4 项定向 Electron E2E，覆盖关于页版本显示、扫描 PDF 图片区域提问及追问、视觉 OCR 和最近对话恢复，均通过。
- PDF 工具与助手栏的浅色、深色和窄窗口界面已在实际 Electron 应用中检查。

## 待发布文件

忽略的本地目录 `release/v0.6.0-publish/` 包含以下文件，供正式发布时上传：

| 文件 | 字节数 |
| --- | ---: |
| `LLM-Reader-0.6.0-setup.exe` | 124,603,042 |
| `LLM-Reader-0.6.0-setup.exe.blockmap` | 131,458 |
| `latest.yml` | 349 |

安装包 SHA-256：`79A5472D063CC6928716009D177A6689CE3B64CE696FBD3A78A421D974A7F8A8`。已核对待发布副本与构建原件的 SHA-256 一致，且其 SHA-512、文件名和大小与 `latest.yml` 匹配。Windows Authenticode 检查结果为 `NotSigned`。

## 发布前仍需完成

- 在隔离的 Windows 用户环境中实际运行安装、从 v0.5.0 升级和卸载，检查用户数据保留及自动更新。
- 用代表性的扫描件、复杂论文和实际配置的视觉模型或文档服务验收识别质量。模拟接口测试仅证明流程和协议行为。
- 确认发布说明后创建 `v0.6.0` 标签和 GitHub Release，并上传上表三个文件；发布后复核下载、更新清单与公开版本状态。
