# 重排真实服务补测

2026-09-08 14:40–14:45（北京时间），在前次自动验收完成后，使用本会话已获授权的凭据补测。真实重排服务可用，五个代表问答的结论与引用一致；当前对照样本三组均已满分，尚未证明重排带来额外质量收益。

## 环境与范围

- 程序：直接启动 `release/win-unpacked/LLM Reader.exe`，版本 0.4.0。实测前核对 `app.asar` SHA-256 与原验收记录一致：`C266F908A1F88CF4D33F47A8E7C6A1A1510A30234AEA81595189B3E12BF67B84`。
- 接口：硅基流动 `BAAI/bge-reranker-v2-m3`；完整问答另用 `BAAI/bge-m3` Embedding 和已保存 Go 配置的 `deepseek-v4-flash`。
- 数据：隔离书库中的自建 TXT 和两页扫描 PDF。只向接口发送这些测试样本，没有使用个人书籍，没有更改日常模型或知识处理配置。
- 本次只补测并更新记录，未修改产品代码；256 项单元测试、70 项 Electron 测试及 Windows 构建仍引用前次结果，没有冒充本次重跑。

## 24 题真实重排对照

执行既有 `scripts/rerank-eval.mjs --live`，使用同一份六章、192 个块、77,907 个 Unicode 字符的语料。此组只测试 SQLite FTS 取材，固定章节笔记与规划回退，不生成完整回答。四个无答案问题不计入原文召回分母。

| 组别 | 题数 | 候选原文命中 | 最终原文命中 | 缩减后原文命中 | 章节覆盖 / 缩减后 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 当前检索 | 24 | 28/28 | 28/28 | 28/28 | 25/25 · 25/25 |
| 扩大候选 | 24 | 28/28 | 28/28 | 28/28 | 25/25 · 25/25 |
| 扩大候选加真实重排 | 24 | 28/28 | 28/28 | 28/28 | 25/25 · 25/25 |

真实组 24/24 次成功，无回退；候选数 13–47。耗时最小 207 ms，中位 579 ms，P95 847 ms，最大 997 ms；中位数取中间两值平均，P95 使用最近秩。三组同为满分说明本样本未发现退步，也暴露出样本难度不足，不能据此宣称质量改善。

逐题原文和指标：[real-results.json](../output/rerank-evaluation/real-results.json)。其中 `fullAnswerReview` 仍为未验证，专指这 24 题没有逐题生成回答；下面另记五个完整问答。

## 打包程序中的五个真实完整问答

先通过实际 IPC 配置并测试独立 Embedding、重排服务，再建立 TXT 192/192、PDF 11/11 的真实语义索引。章节笔记使用固定自建内容；PDF 原文复用此前 MinerU 成功提取的样本，没有重新调用 OCR 或模型分析。通过实际界面提问，每题均为独立对话；本轮实际使用混合检索和真实 Go 回答。

| 问题 | 候选数 | 重排耗时 | 完整问答耗时 | 人工证据复核 |
| --- | ---: | ---: | ---: | --- |
| q01 定义 | 60 | 766 ms | 4.530 s | 定义、别名和与投票的区别分别由原文支持 |
| q10 紧急例外 | 60 | 1109 ms | 16.970 s | 保留立即危险、不能等待、可逆措施、临时标注和事后复核 |
| q17 跨章比较 | 58 | 883 ms | 8.848 s | 通常门槛、紧急例外及不可逆措施的限制均有对应依据 |
| q21 书中无答案 | 52 | 777 ms | 6.608 s | 明确说明未报告错误率降幅，没有编造数字 |
| PDF 表格 | 11 | 266 ms | 6.461 s | A 组 3 人、B 组 1 人、相差 2 人；保留虚构测试数据的原注 |

五次重排全部应用成功。回答中的 13 处引用编号全部属于本轮实际发送原文，逐句核对后未发现无依据扩写。每题点击首个引用且没有定位错误提示，随后保存归档并验证保存的重排状态。PDF 引用锚点为第二页，截图中的原表行列、数值与回答一致。此次检查不将整体问答耗时归因于重排，最长回答接近 17 秒，而该次重排约 1.1 秒。

人工目视检查了跨章比较与 PDF 表格截图：引用可读，正文定位正确，回答区域没有明显溢出。完整回答、上下文与请求状态见 [answers.json](../output/rerank-validation/live/answers.json)；截图见 [跨章比较](../output/rerank-validation/live/q17.png) 和 [PDF 表格](../output/rerank-validation/live/pdf-table.png)。请求记录只含服务域名、路径、方法及 HTTP 状态，不含凭据或认证头。

## 结论边界

本次补齐了真实服务连通性、重排延迟，以及少量真实混合检索与完整回答的验收。五个回答均有依据；前述 24 题没有显示额外检索收益。尚未对真实长书、更多难负例或复杂表格开展对照，也未重测新一轮章节分析和 OCR。保留独立可选配置及五秒回退是合适的现阶段交付，质量提升需由更难且有人工标注的样本继续证明。

清理状态：测试应用已关闭。删除本次隔离测试目录 `C:\Users\wrh37\AppData\Local\Temp\llm-reader-rerank-live-ukAjbu` 的操作被自动审批拒绝，返回 `blocked by policy`，未说明更具体原因。目录仍保留，未通过其他方式重试；正式验收结果与截图另存于上述 `output` 路径。
