# 全书上下文交付状态

更新：2026-09-09。本次交付范围为源码提交与推送；按用户要求暂停安装包构建，不安装或发布。

## 已落地

- 原文准备与可选章节笔记分开。EPUB/TXT 本地提取，PDF 接入 MinerU 本地、MinerU 云端和 Docling；原文就绪即可进行本段或全书问答。
- 原文结构保存章节层级、逻辑单元、脚注、表格及来源范围；章节笔记、概念索引和全书概要只作为背景。
- SQLite FTS 全文检索、可选 Embedding 向量和混合检索；向量存入本机 SQLite，无需独立向量数据库服务。
- 独立证据重排配置、一次取材规划、五秒重排回退、证据预算和逐轮引用快照；关联内容只使用剩余名额。
- OpenCode Go 官方地址自动识别和中转手动适配；统一真实客户端标识，以及在问答、归档和分析续跑中稳定的独立会话 UUID。
- 原文、笔记、向量任务分别保存状态，支持取消、用户续跑和明确重建；旧数据库、归档与原文引用保持兼容。
- 审查发现的两处问题已修复：关联脚注挤掉直接证据，以及 MinerU 主体为空时丢失仍有效的说明文字。详见 [修复验收](document-structure-fixes-validation.md)。

## 已验证

本次最终 `pnpm test:all` 通过：lint、typecheck、286 项单元测试、Electron 应用编译、72 项 Electron E2E。另有 1 项需要个人 PDF 的既有测试跳过。自动测试使用本地模拟接口，不访问用户真实模型账户。

本会话此前的独立真实服务记录已经覆盖 Go 请求头与会话、硅基流动 Embedding、MinerU 云端两页扫描 PDF，以及真实重排 24 题和五个完整问答。这些记录分别对应当时的程序和样本，不能视为最新文档结构实现的全部联网复验：

- [Embedding 与 Go 真实接口](knowledge-live-validation.md)
- [MinerU 云端真实扫描 PDF](mineru-live-validation.md)
- [真实重排对照与五个完整问答](rerank-live-validation.md)
- [新结构确定性评测及两道原文问答](document-structure-validation.md)

## 尚未完成的验证与后续工作

1. **新版结构的真实复杂 PDF 验证。** 最新 MinerU/Docling 结构保留与本次文字遗漏修复目前由模拟产物和回归测试验证；还需真实双栏、跨页表格、公式、脚注及扫描材料对照。Docling 和 MinerU 本地服务尚无真实服务验收。
2. **更难的长书质量对照。** 已完成的 24 题真实重排中各组都已满分，尚不能证明额外收益；仅五题生成并人工核对完整回答。仍需更长书籍、难负例、隐含关联和复杂表格样本，比较全文、混合检索、重排的证据与完整回答质量。
3. **本次安装包与发布。** 最新修复没有成功生成并验收安装包；用户已明确暂缓。以后需要发布时再构建并进行打包程序验收，不能使用前一阶段安装包代表本次源码。

已准备过且受说明遗漏影响的 PDF，需由用户点击“重新准备原文”更新缓存；可用原始解析产物会在本机复用。该操作使相关笔记和语义索引待重建，已有归档保留。问答证据优先级修复无需重建原文。

图片理解、扫描页整页 OCR 文本覆盖层、跨书检索，以及自动安装解析或向量服务仍不在当前交付范围。临时验证产物、个人配置和本机工作计划保留在本地，不作为本次源码交付。
