# 全书上下文真实模型对照记录

2026-09-06 14:57–15:03，使用已保存的 `deepseek-v4-flash` 配置完成一次真实接口对照。分析与问答使用同一配置，六个固定问题分别在分析前、分析后独立作答；各轮不带历史。这里记录单次样本的证据与回答检查，不是统计性的模型准确率或人工内测结论。

## 样本与复现

- 样本：自建《分证判断评测样本》，三章、8,962 个 Unicode 字符，提取为五节。原文定义了虚构的“分证判断”“双证门槛”和紧急行动例外，并包含较长的整理记录。
- 样本版本：`1`；SHA-256：`224f9e500f6d86116d8eb32db29f7f5c8f27f07b596f5b1632ef1c203fbb3d58`。
- 固定原文和问题在 `scripts/book-context-fixture.mjs`。脚本先通过真实阅读视图选中第三章结语，取得原有的同章、最多 6,000 字局部上下文。该章本身较短，实际包含三处原文；没有人为填满到 6,000 字，也没有给旧基线提供其他章节。
- 增强组中的定义、别称、比较、隐含问法使用本段模式；全书论证和无答案问题使用不带选区的全书模式。
- 原始逐轮记录保存在本机 `tmp/book-context-eval-1788677878142/report.json` 和 `report.md`，包含回答、实际来源、引用编号、覆盖范围和已返回用量，不含 API Key。评测使用隔离临时书库，未读写日常书库和配置。

复现前运行 `pnpm build:app`，再运行 `pnpm eval:book-context` 检查条件；显式添加 `--run` 会使用所选真实接口并消耗模型额度。它没有加入 `pnpm test:all`。

## 关键证据与逐项复核

下表的分子是实际发送给回答模型的关键原文数量，分母是该题预先指定的关键原文数量。“引用”另外检查回答是否引用了这些原文；编号存在本身不代表语义正确。

| 问题类别 | 旧局部取材 | 增强取材 | 增强回答引用关键原文 | 对照原文后的观察 |
| --- | --- | --- | --- | --- |
| 跨章定义 | 0/1 | 1/1 | 1/1 | 准确说明分开处理个人立场与可核查事实，并要求交代材料来源。旧组明确缺少定义。 |
| 同义问法“拆开查证” | 0/1 | 1/1 | 1/1 | 正确关联原文别称与“分证判断”。旧组没有猜测术语含义。 |
| 双证门槛与投票的章节比较 | 0/2 | 2/2 | 2/2 | 区分记录与独立复核的证据条件，以及投票的议程、偏好功能。主要比较有原文支持。 |
| 全书论证及临时行动例外 | 1/3 | 3/3 | 3/3 | 连接概念、证据门槛、可逆行动及事后复核；保留“行动时机例外不等于结论已证实”的限制。 |
| 不存在的错误率降低数据 | 不适用 | 不适用 | 不适用 | 两组均说明没有数值或实验依据，未编造百分比。增强组引用门槛定义及明确不报告实验数据的整理记录。 |
| 隐含问法“一窝蜂赞成，怎样判断靠不靠谱” | 0/3 | 3/3 | 2/3 | 取得定义、门槛、投票三处原文，回答引用前两处。结尾“至多只能算待验证假设”是推断，未明确标注为推断。 |

十二轮回答均未出现不在本轮来源映射中的 `[P数字]` 引用。增强组五道有答案问题的预设证据全部取得；隐含问法并未引用所有已取得的证据，因此没有把“召回完整”写成“引用完整”。原始回答及每轮不同的 P 编号映射保留在本地报告中。

## 用量与限制

接口已返回的用量：分节分析及汇总 **17,060 tokens**，六轮旧基线 **4,910 tokens**，六轮增强问答 **24,681 tokens**（包括已返回的取材规划用量），合计 **46,651 tokens**。六轮增强中有四轮记录了规划用量，另外两轮没有记录；启动连接探测也未纳入此合计，因此这不是完整计费账单，不能推算总费用。

本轮验证表明章节准备和重新取材能补回该样本的跨章证据，并能帮助模型维持“缺少实验数据就不报数字”的边界。但样本只有三章，规则明确且重复内容较多。隐含问法使用了包含术语的段落选区，不能据此宣称任意同义表达已经解决；先前纯全文检索对未收录别称“跟风”的漏检仍保留在基线记录中。

后续真实长书评测应继续关注未收录别称、多主题章节中的证据排序、是否明确标注推断，以及上下文增加后的调用用量。本期仍以结构与全文检索为基线；是否加入混合检索，需要更多真实漏检样本支持。
