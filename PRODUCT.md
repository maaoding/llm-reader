# LLM Reader 产品契约

## 定位

LLM Reader 是一个 Windows 优先、本地优先的桌面阅读器，帮助用户理解复杂非虚构内容。核心价值不是“在阅读器旁放一个聊天框”，而是让每次解释、追问和收藏都与原文位置保持可靠联系。

## MVP 闭环

### 页面与阅读工作台

- 全局入口为书库、回答归档、设置。顶栏在入口右侧排列已打开书籍的标签（显示书名，宽度不足时横向滚动）：打开一本书即新增标签，点击标签切换书籍并回到该书上次所在的书内页面，关闭标签回到书库且保留该书草稿与归档，标签可拖拽调整顺序；标签列表随工作区状态持久化，书籍被删除时其标签同时消失，超过上限的标签不再恢复。书库使用封面在左、书籍信息在右的紧凑横向卡片，1180px 及以上三列、940–1179px 两列；卡片显示书名、作者（未知作者时明示）、书籍格式、阅读进度及独立详情入口；书名最多两行且预留两行高度，网格行高保持统一，支持书名与作者搜索，按最近打开排序。打开书籍和单本导入进入概览；批量导入完成留在书库。重启恢复上次书籍、页面与标签列表。
- 单本工作台包含概览、阅读、章节笔记、对话。每本书只保留最后一个临时会话（草稿 + 未归档轮次）并写入本机数据库：重启后恢复草稿、回答与原会话标识，开新会话会覆盖旧记录，删除书籍会随之清理；“清空会话”控件可手动丢弃。打开的会话标签列表（含归档标签及其草稿、当前激活标签）同样随重启恢复，最多恢复最近 20 个并优先保留当前激活标签；已删除的书籍或归档对应的标签不再恢复。每个标签各自记住停在概览、阅读或章节笔记的哪一页；助手对话页是跨书的完整对话工作台，不计入标签页面。概览提供继续阅读、开始提问、独立准备状态、已有全书概述和最近保存的五条回答（按 Markdown 渲染行内强调，丢弃块级结构后截断）。同书切页保持同一个阅读器实例及会话草稿，不重新打开书籍或发起问答；切换标签会重新打开目标书籍。
- 阅读页的书名横栏同时显示当前章节、百分比和进度条，正文紧接横栏开始；左侧固定窄图标栏提供目录、摘录、搜索、排版／显示和书籍详情，目录、摘录、搜索从图标栏右侧共用无重复切换横栏的抽屉展开，再次点击当前图标即可收起。只有偏离自然位置时在图标栏下方显示“返回阅读处”。“本书准备”保留为横栏常显文字按钮。PDF 使用“显示”，集中放置缩放、适合宽度、纸张设置和受支持的框选文字。1180px 及以上默认停靠右侧助手，940–1179px 下阅读页仍保留右侧助手并为它让出宽度，左侧目录、摘录、搜索以抽屉覆盖正文。
- 助手范围为选中内容、整本书，新会话默认整本书，划词操作切换至选中内容。完整对话页把范围控件放在输入框上方，不显示独立准备横栏；准备或缺少条件状态合并到发送提示并提供对应入口。侧边助手保留范围、状态和准备入口，与完整对话共用同一会话和草稿。尚缺模型或原文时可以写草稿。每个会话同时只允许一个进行中的请求，切换书籍或标签不会中断它，删除书籍或退出应用才取消。会话请求最多两个并发（跨书共享），超出上限的在发送后排队等待。连接状态点按“是否还有请求在跑”聚合：并发时单个请求失败不会把状态点标成断开，只有没有任何请求在进行时才按结果标记。每轮回答以“本次参考原文 · N 处”折叠展示来源，笔记覆盖与排序情况放在展开详情；操作名为“保存回答”。回答归档直接提供跨书搜索、继续追问和 Markdown 导出。从归档条目继续追问时先进入对话页、目标书籍在后台打开，不经过书籍页面。对话页与归档页是和书库同级的独立页面：没有关闭按钮、不做入场动画，通过顶栏书库或书籍标签离开。
- “本书准备”固定显示书名，分别展示原文准备、章节笔记、按含义查找的状态、进度、操作和错误。原文就绪即可整本书提问，笔记与含义查找均可选。处理检查和失败记录折叠呈现；启动附近说明外部处理与额度，重建确认说明具体影响。服务入口定位对应设置，返回后恢复原书面板。
- 全书概览、真实章节汇总及每条笔记概述使用公共 Markdown 展示，支持标题、段落、粗体、斜体、列表、引用、行内代码和代码块；它不解析回答引用，HTML、图片和外链保持普通文本。章节笔记只读区分概述、观点、适用条件、例外、概念和经校验的原文来源；缺少的汇总不生成占位内容，同名章节保持独立身份。每次读取最多10条笔记，失效来源提示且不可点击。读取沿用现有 SQLite 表，无迁移，不新增生成或编辑流程。
- 设置按外观、阅读、划词操作、模型服务、阅读增强、关于分组。阅读增强服务分别成卡，测试结果显示在对应卡片，校验失败定位输入项。应用自有文案集中于 `src/shared/copy.md`，通过类型化 key 与占位符契约校验；用户自定义名称、提示词、书籍内容和历史回答保持原样。

### 阅读与数据流程

1. 导入无 DRM 的 EPUB、UTF-8 TXT 或 PDF；本机已安装 Calibre 时，也可将无 DRM 的 MOBI/AZW3 转换为 EPUB 后导入。支持一次选择或拖入最多 300 个文件批量导入：逐本顺序处理、单项失败不中断批次，可取消（当前项完成后停止，未处理项计为跳过）；单本导入自动打开，多本导入完成留在书库并显示汇总。书库支持删除书籍，删除会一并清除该书的摘录与归档，且不可恢复。
2. 连续滚动阅读、记录自然阅读位置并恢复上次位置；目录、引用和收藏跳转不会覆盖它。
3. 支持在当前书内全文搜索，命中可逐个跳转且不破坏自然阅读位置。
4. 阅读区提供随界面明暗切换的默认、护眼纸张主题；阅读排版可调整正文字号、系统字体、行间距、首行缩进、正文宽度、段落间距与对齐；书名横栏显示当前章节与本章进度。
5. 选中原文，选择“解释这段”“联系上下文”“自由提问”或“收藏”；前三种划词操作的名称、图标与固定提示词可在设置中由用户自定义。
6. 划词收藏的句段持久高亮，并可从左侧“收藏”栏跳回原文。
7. EPUB/TXT/PDF 提供独立的“准备原文”与“生成章节笔记”入口。EPUB/TXT 准备在本机完成，无需分析模型；PDF 准备只要求用户配置文档服务。整份原文提取、结构校验与 SQLite 全文索引完成后即可书内问答。章节笔记为可选步骤，只处理已准备原文；启动前说明分节原文会发送至所选模型。分析配置与当前问答配置独立，默认当前配置。笔记显示分节分析、章节汇总与全书合并进度，支持暂停、用户续跑与重建；临时错误及结果校验失败每步最多尝试3次。保留最近10条不含正文或凭据的失败记录，以及已完成笔记、章节和中间汇总，退出后不自动继续外部调用。
8. 助手提供“本段／全书”切换。准备期间仍能解释选区；原文可检索后，全书问答无需选区且只检索当前书。笔记未开始、部分完成、失败或暂停均不阻断已就绪原文。使用本地 SQLite FTS5、中文分词、汉字二元词与可用笔记中的概念别名；每次问答至多一次取材规划，无笔记时使用目录与问题，失败回退本地检索。目录预算不足只截断规划输入，不缩小全文检索范围。各轮重新取材，原文、摘要和历史共用输入预算；部分笔记可作背景，只报告实际笔记覆盖，无全书概要时不生成占位概要。摘要与历史回答不作为原文证据。
9. 兼容普通 `/v1/chat/completions` 文本消息，不要求工具调用；流式显示回答，回答前提供本轮实际原文来源，引用可跳回原文；无法验证的引用必须明确标记，上下文缩减重试同步替换引用映射。模型配置支持“自动／OpenCode Go”请求适配：官方 Go 地址自动识别，中转可手动指定。统一发送真实客户端版本；Go 请求携带稳定的随机会话 ID，覆盖问答、规划、分析与连接探测，追问、归档重开、重试和分析续跑时复用，重建分析时更换；Go 请求不自动跟随重定向。不提供任意自定义请求头或其他 API 协议。
10. 用户主动把有价值的回答归档为本地会话；归档可在完整对话页中继续追问，逐轮保存实际证据及来源范围。旧归档无需重建仍可读、追问、导出。原文准备、章节笔记与语义索引分别保存状态和失败原因：重新准备原文重建结构并使笔记、向量待重建；重新生成章节笔记只重建笔记；重建语义索引只重建向量。配置编辑只暂停受影响任务，重建原文和删书取消关联请求并拒绝过期回调；Go 会话 ID 的问答、续跑和重建生命周期保持兼容。
11. 助手工作台提供跨书归档总览、搜索，以及按全部、本书或单条导出为 Markdown（包含每轮引用原文）。
12. 应用启动时静默检查更新，设置页打开且尚未检查时自动补一次检查；发现新版本后展示更新日志摘要（截断为纯文本），由用户确认下载，重启后完成安装。更新源为固定的 HTTPS 地址，更新包完整性校验失败时不安装，也不静默安装或强制重启。

## 证据重排

- “设置 → 阅读增强”在 Embedding 与 PDF 文档处理之间提供独立的证据重排地址、模型、密钥、启用和测试。新用户及升级默认关闭且地址、模型为空；保存并启用后自动用于适用的后续问答。测试发送当前未保存表单及固定短文本，不上传书籍；页面说明问题和候选原文会发送到所选服务。
- 兼容文本 `POST <baseUrl>/rerank`，使用独立 Bearer 凭据和真实应用版本标识，不附带 Go 会话头，不跟随重定向。每轮最多一次，包含正文读取在内最多等待 5 秒、响应最多 1 MiB，不自动重试。异常回退本轮融合排序；取消和删书终止整轮问答。保持既有问答总超时。
- 沿用一次取材规划。原文就绪的书籍进入书内检索后，全文和向量各取最多 40 条，按文档块编号去重并合并可用章节笔记指向的有效原文，最多 60 条参与重排；没有可用语义索引时可单独重排全文结果。查询包含问题或动作默认提示、已有规划词及最多 600 字选区，共最多 2,000 个 Unicode 字符。候选只含标题路径和原文，不含摘要或历史回答。
- 两条以上候选才请求服务；选区之外最多 12 条补充原文，优先保留最多两条邻近原文及最多六个目标章节的代表。排序后可补充同一逻辑单元的邻接原文，以及有明确关联的脚注、表注。章节按编号分组，标题和原文可重复；没有候选的章节不伪造来源。最终输入预算优先保留选区、命中、邻近及跨章代表，可选扩展空间不足时舍弃；必要时缩短代表原文并同步裁剪来源范围、表格行和单元格字符范围，引用编号最后生成。
- 取材开始时固定本轮重排配置。上下文缩减重试、流式降级复用取材及排序，每次实际输入重新分配引用编号。来源折叠区显示重排应用、跳过或回退结果；受控记录随本轮快照归档。旧归档仍可读取、追问和导出。重排配置不会使已有分析或向量失效，也不改变文档任务；不重新分析书籍、不建立独立向量数据库。

## 全书上下文二期

- 设置提供“阅读增强”，Embedding 与 PDF 文档处理配置独立于问答、章节分析配置；可测试当前未保存表单，密钥由主进程使用 safeStorage 加密保存，Renderer 只获得已保存密钥的存在性。
- 语义检索默认关闭，使用兼容 `/v1/embeddings` 的接口。原文准备就绪后，用户手动建立索引，无需先生成章节笔记；标题路径与原文分批发送至 Embedding 接口，向量以归一化 Float32 存入本机 SQLite。支持独立进度、暂停、续跑、重建；退出后不自动续跑。更换接口、模型或密钥使索引待重建，不使章节笔记失效。单书向量上限 128 MiB、维度上限 4096。
- 问答只在当前书内融合全文检索与余弦相似度结果，保留语义检索的独有候选，补足同义表达漏检；每次问题及选区可发送到所选 Embedding 接口，失败回退全文检索。向量和摘要均不替代原文证据，引用仍限定本次实际上下文。
- PDF 可选 MinerU 本地 pipeline 异步 API、MinerU 云端 pipeline 批量文件 API 或 Docling Serve 异步 API。用户从“准备原文”主动发送整份 PDF；先在 sandbox Renderer 校验实际页数，再由主进程调用服务。支持 OCR、语言选择，保留实际阅读顺序、标题层级、脚注、公式和表格结构。原文就绪后进入同一问答流程，章节笔记可另行生成。识别文本不替换原 PDF，不改变阅读视图的选择能力。
- PDF 处理上限 200 MB、600 页；任务编号、最多 48 MB 原始解析产物与最多 96 MB 规范化结构本地缓存，派生分块可重新建立。暂停停止本机等待，远端可能继续处理；续跑复用已接受任务，不自动重复上传。任务失败、过期或提交结果不确定时需用户明确重新准备。同一服务配置且产物可用时重建复用产物，否则明确重建后才重新上传。已有归档快照保留；删书同时取消和清理关联任务。
- 文档及 Embedding 请求使用真实应用版本标识，拒绝自动重定向，错误不回显正文、密钥或远端原始错误。云端预签名上传和下载不携带供应商密钥；只接受已知 MinerU 存储域名或用户明确填写的本机模拟地址。第三方结果的结构、大小、页码与引用均校验；不执行返回的 HTML，也不访问结果中的图片或任意链接。

## 文档结构与兼容

- 文档树保存父子关系、层级和阅读顺序；原文单元保存独立文字、类型、归属与来源。模型处理分节仅为有大小限制的批次，不充当真实章节。普通原文块最多 1,800 个 Unicode 码点，处理分节最多 6,000 个码点，优先句子边界，不跨真实小节拼接。超长单元保留拆分前的共同归属。
- EPUB 优先合并目录、片段锚点与同位置正文标题，保留多级关系；缺目录时用正文标题，仍缺结构时按文件顺序使用“文本分节”。沿用阅读器清理规则与 CFI。TXT 复用段落和标题识别，仅明确 Markdown 级别、卷/章/节等编号建立层级，其他标题平级，无标题使用“文本分节”，位置按 Unicode 码点计算。重复文字和同位置不同单元保留不同编号，不按正文哈希合并。
- Docling 使用正文树；MinerU 只使用服务实际返回的标题层级，无层级时平级。保留所有有效来源页、坐标及坐标系。明确页眉页脚独立分类、保留来源且默认不参与正文检索；未知结构保留文字。表格、脚注、表注、标题和公式独立保留；只有显式来源依据才能建立关联，不猜测跨页表格关系。
- 表格保存单元格、真实表头、行列跨度、标题与表注。MinerU HTML 使用生产依赖 `parse5@8.0.1` 纯语法树解析，只读取表格与文字。结构损坏但文字/位置仍有效时降为文本并提示。按完整行分块并重复真实列头；超长单行必须切分时保存单元格字符范围。超长表头本身单独切块保留，无行级页码时只提供整表页范围。
- 页级诊断覆盖缺少正文、未知结构、无法关联脚注、表格降级与疑似重复。诊断只提示，不据此删除正文；界面集中显示在可折叠的“文档检查”中。不得将这些指标称为 OCR 准确率。来源折叠区分别显示笔记覆盖与本次实际证据，并为多页来源提供逐页跳转和整表精度提示。
- SQLite 迁移 14 将原文、全文索引和语义索引的生命周期从章节笔记中分离。旧已完成提取的缓存即使全书概要失败也可恢复原文问答；旧结构继续按版本 1 使用，只有用户重新准备才补齐新结构。未改变输入的旧向量、已有归档、逐轮快照、引用和导出保持兼容；升级不自动上传、分析或建立向量。原文准备与笔记共享原有单任务限制，语义索引沿用原有限制，外部调用继续让位于问答，不新增队列。

## 成功标准

- 内测用户无需指导即可走完完整闭环。
- 关闭并重启应用后，书籍、自然阅读位置、摘录、回答归档（含追问历史）、每本书的临时会话与打开的会话标签仍然存在。
- API Key 不以明文进入数据库、日志或渲染进程持久状态。
- EPUB/PDF 内脚本、外部导航、附件和权限请求无法触达 Electron/Node 能力。

## 明确不做

不内置或自动安装 Embedding 模型、MinerU、Docling、OCR 引擎或独立向量数据库；外部服务由用户自行配置。不增加解析后端、图片理解、本地 PDF 自动解析路线或整书 LLM 语义切分。扫描页 OCR 仅用于原文检索与可选笔记，不新增扫描页划词、OCR 覆盖层、跨页选区、表单、PDF JavaScript、附件执行或外部链接支持。账号、同步、计费、遥测、自动批注、TTS、插件与代码签名仍不在范围内。
