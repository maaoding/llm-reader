# LLM Reader 产品契约

## 定位

LLM Reader 是一个 Windows 优先、本地优先的桌面阅读器，帮助用户理解复杂非虚构内容。核心价值不是“在阅读器旁放一个聊天框”，而是让每次解释、追问和收藏都与原文位置保持可靠联系。

## MVP 闭环

1. 导入无 DRM 的 EPUB 或 UTF-8 TXT。
2. 连续滚动阅读、记录自然阅读位置并恢复上次位置；目录、引用和收藏跳转不会覆盖它。
3. 阅读区提供浅色、米黄、深色纸张主题，且独立于界面主题；标题栏显示本章进度。
4. 选中原文，选择“解释这段”“联系上下文”“自由提问”或“收藏”；前三种划词操作的名称、图标与固定提示词可在设置中由用户自定义。
5. 划词收藏的句段持久高亮，并可从左侧“收藏”栏跳回原文。
6. 仅把选区及当前章节的有限上下文发送给用户配置的 OpenAI 兼容接口。
7. 流式显示回答，引用可跳回原文；无法验证的引用必须明确标记。
8. 用户主动把有价值的回答归档为本地会话；归档可在大号助手对话页中继续追问，追问历史持久化保存。

## 首版成功标准

- 内测用户无需指导即可走完完整闭环。
- 关闭并重启应用后，书籍、自然阅读位置、句段收藏和回答归档（含追问历史）仍然存在。
- API Key 不以明文进入数据库、日志或渲染进程持久状态。
- EPUB 内脚本、导航和权限请求无法触达 Electron/Node 能力。

## 明确不做

PDF、OCR、整书问答、向量数据库、账号、同步、计费、遥测、自动批注、TTS、插件、自动更新、代码签名与笔记导出均不在此版本范围内。
