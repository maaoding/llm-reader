# LLM Reader 产品契约

## 定位

LLM Reader 是一个 Windows 优先、本地优先的桌面阅读器，帮助用户理解复杂非虚构内容。核心价值不是“在阅读器旁放一个聊天框”，而是让每次解释、追问和收藏都与原文位置保持可靠联系。

## MVP 闭环

1. 导入无 DRM 的 EPUB、UTF-8 TXT 或 PDF；本机已安装 Calibre 时，也可将无 DRM 的 MOBI/AZW3 转换为 EPUB 后导入。支持一次选择或拖入最多 300 个文件批量导入：逐本顺序处理、单项失败不中断批次，可取消（当前项完成后停止，未处理项计为跳过）；单本导入自动打开，多本导入完成留在书库并显示汇总。书库支持删除书籍，删除会一并清除该书的句段收藏与归档，且不可恢复。
2. 连续滚动阅读、记录自然阅读位置并恢复上次位置；目录、引用和收藏跳转不会覆盖它。
3. 支持在当前书内全文搜索，命中可逐个跳转且不破坏自然阅读位置。
4. 阅读区提供浅色、米黄、深色纸张主题，且独立于界面主题；阅读排版可调整正文字号、系统字体、行间距、首行缩进、正文宽度、段落间距、页边距与对齐；标题栏显示本章进度。
5. 选中原文，选择“解释这段”“联系上下文”“自由提问”或“收藏”；前三种划词操作的名称、图标与固定提示词可在设置中由用户自定义。
6. 划词收藏的句段持久高亮，并可从左侧“收藏”栏跳回原文。
7. 仅把选区及当前章节的有限上下文发送给用户配置的 OpenAI 兼容接口；支持保存多套命名配置、切换当前使用的配置，并可从接口拉取模型列表辅助填写。
8. 流式显示回答，引用可跳回原文；无法验证的引用必须明确标记。
9. 用户主动把有价值的回答归档为本地会话；归档可在大号助手工作台中继续追问，追问历史持久化保存。
10. 助手工作台提供跨书归档总览、搜索，以及按全部、本书或单条导出为 Markdown。
11. 应用启动时静默检查更新，设置页打开且尚未检查时自动补一次检查；发现新版本后展示更新日志摘要（截断为纯文本），由用户确认下载，重启后完成安装。更新源为固定的 HTTPS 地址，更新包完整性校验失败时不安装，也不静默安装或强制重启。

## 首版成功标准

- 内测用户无需指导即可走完完整闭环。
- 关闭并重启应用后，书籍、自然阅读位置、句段收藏和回答归档（含追问历史）仍然存在。
- API Key 不以明文进入数据库、日志或渲染进程持久状态。
- EPUB/PDF 内脚本、外部导航、附件和权限请求无法触达 Electron/Node 能力。

## 明确不做

PDF 首版仅支持连续页浏览、适合宽度、缩放、页码进度、文本搜索和单页文字选择；扫描页可浏览但不提供 OCR，跨页选区、表单、PDF JavaScript、附件执行和外部链接均不支持。OCR、整书问答、向量数据库、账号、同步、计费、遥测、自动批注、TTS、插件与代码签名均不在此版本范围内；代码签名在项目获得足够关注后再评估。
